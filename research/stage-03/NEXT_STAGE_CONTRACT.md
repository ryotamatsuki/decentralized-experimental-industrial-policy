# Stage 3 → Human Hard Gate Contract

## Stage 3 result

- Canonical verdict: `GO`
- Routing/status: `GO TO MINIMAL MODEL`
- Preferred candidate: **C4 — Competition to host a supplier’s scarce launch trial**
- Alternatives: C6, C3

## Hard stop

This run terminates here.

**Stage 4 is not authorized until the researcher explicitly selects a candidate.**

No agent may interpret the Stage 3 `GO` as permission to:

- build formal payoffs/utility/demand;
- solve an equilibrium;
- derive FOCs/SOCs;
- prove propositions;
- run numerical equilibrium simulations;
- create a Stage 4 directory;
- write a manuscript;
- add mechanisms to rescue C4 if it fails.

## If the researcher approves C4

The next run may execute only the canonical Stage 4 Minimal Model Gate using `research/stage-03/STAGE4_HANDOFF.md` as the project-specific input and the then-current `research-paper-workflow` canonical template.

It must:

1. re-check the closest literature relevant to the exact C4 skeleton before claiming novelty;
2. construct the smallest model separating trial occurrence from trial location;
3. test the non-additionality and under-experimentation conjectures rather than assume them;
4. use symbolic verification when applicable;
5. return `GO`, `CONDITIONAL GO` with exactly one repairable deficiency, or `NO-GO`;
6. stop/route according to the canonical Stage 4 rules.

## If C4 fails

Do not automatically move to C6 or C3. Human routing is required. A Stage 4 `NO-GO` cannot be rescued by adding multiple margins.

## Mandatory status

`STOPPED AFTER STAGE 3 — HUMAN APPROVAL REQUIRED BEFORE STAGE 4`
